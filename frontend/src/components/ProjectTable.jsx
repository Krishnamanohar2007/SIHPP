import RiskBadge from "./RiskBadge";
export default function ProjectTable({ projects, onSelect, sort, setSort }) { const ordered = [...projects].sort((a,b) => String(a[sort]).localeCompare(String(b[sort]))); const head = (label, key) => <th onClick={() => setSort(key)} className="cursor-pointer px-4 py-3 text-left text-xs font-bold uppercase tracking-wider text-slate-400">{label}{sort === key ? " ↓" : ""}</th>; return <div className="overflow-x-auto rounded-xl bg-white shadow-sm ring-1 ring-slate-200"><table className="w-full min-w-[760px]"><thead className="border-b border-slate-100 bg-slate-50"><tr>{head("Project", "project_name")}{head("Type", "project_type")}{head("State", "state")}<th className="px-4 py-3 text-left text-xs font-bold uppercase tracking-wider text-slate-400">District</th>{head("Risk", "risk_category")}{head("Delay", "delay_probability")}</tr></thead><tbody>{ordered.map((project) => <tr key={project.project_id} onClick={() => onSelect(project)} className="cursor-pointer border-b border-slate-100 last:border-0 hover:bg-slate-50"><td className="px-4 py-4 font-semibold text-slate-800">{project.project_name}</td><td className="px-4 py-4 text-sm text-slate-600">{project.project_type}</td><td className="px-4 py-4 text-sm text-slate-600">{project.state}</td><td className="px-4 py-4 text-sm text-slate-600">{project.district}</td><td className="px-4 py-4"><RiskBadge category={project.risk_category} /></td><td className="px-4 py-4 text-sm font-semibold text-slate-700">{Math.round(project.delay_probability * 100)}%</td></tr>)}</tbody></table></div>; }
